<h1>新規タスク登録ページ</h1>

 <%= form_for(@task) do |f| %>
 <%= f.label :content, 'タイトル' %>
 <%= f.text_field :content %>
 
  <%= f.label :status, 'メッセージ' %>
 <%= f.text_field :status %>
  
 <%= f.submit '投稿' %>
 <% end %>
 
 <%= link_to '一覧に戻る', tasks_path %>